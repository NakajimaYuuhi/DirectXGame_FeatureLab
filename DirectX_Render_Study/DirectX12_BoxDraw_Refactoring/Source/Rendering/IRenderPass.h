#pragma once
#include <d3d12.h>
#include <string>
#include "RenderContext.h"

class IRenderPass {
public:
    virtual ~IRenderPass() = default;

    // ?p?X????????iPSO???????K?v????\?[?X??m????j
    virtual void Init(ID3D12Device* pDevice) = 0;

    // ?R?}???h???X?g???R?}???h?????????
    virtual void Execute(const RenderContext& ctx) = 0;

    // ?f?o?b?O??v???t?@?C?????O?p??p?X????O??????????????????????
    virtual std::string GetName() const = 0;
};